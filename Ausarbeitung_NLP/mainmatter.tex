\newpage
\section{Einleitung}
\subsection{Hintergrund (Ronja Rüther)}
In den letzten Jahren sind Chatbots, die auf Large Language Models (LLMs) basieren, wie etwa ChatGPT oder Google Gemini, 
immer beliebter geworden und verzeichnen stetig wachsende Nutzerzahlen. Für viele Personen sind Chatbots zu einem festen 
Bestandteil im Alltag geworden und werden herkömmlichen Suchmaschinen bei der Informationsbeschaffung oft vorgezogen. 
Der Hauptvorteil liegt darin, dass Nutzer schneller eine direkte Antwort auf spezifische Fragestellungen erhalten und 
die eigenständige Suche nach Information auf verschiedenen Webseiten dadurch weitgehend entfällt. \parencite{AIPunk2025}

Diese technologische Entwicklung hat auch im Bereich der Bildung große Veränderungen mit sich gebracht.
Während traditionelle Sprachlern-Apps oft auf statischen Übungen und vorgegebenen Antwortmöglichkeiten basieren,
ermöglichen LLMs die dynamische Generierung von Lerninhalten. Da diese Modelle die natürliche Sprache nicht nur erkennen, 
sondern ebenfalls die Rechtschreibung und Grammatik bewerten können, können sie die Rolle eines digitalen Sprachlehrers 
übernehmen. \parencite{Vorteile_KI_Bildung} 
Dabei erhält der Nutzer individuelles Feedback auf seine Eingaben und kann gezielt auf typische Fehlerquellen
hingewiesen werden. Da diese Interaktion zwischen dem Nutzer und der Künstlichen Intelligenz (KI) in Echtzeit erfolgt, erhält der Nutzer das Gefühl, 
als würde er mit einer realen Person digital kommunizieren. \parencite{Vorteile_KI_Bildung}

Trotz dieser Fortschritte gibt es immer noch Herausforderungen, die diese Modelle mit sich bringen. \parencite{Vorteile_KI_Bildung}
Dazu zählen beispielsweise die individuelle Anpassung der Satzgenerierung an das Sprachniveau des Nutzers sowie die Generierung von 
hilfreichem und nachvollziehbarem Feedback.

\subsection{Stand der Technik (Andreleene Prado)}
Mit der fortgeschrittenen Entwicklung der Künstlichen Intelligenz sowie der zunehmenden Verfügbarkeit von Open-Source-Modellen wie LLaMa gibt es heute deutlich mehr Möglichkeiten,
Chatbots für spezifische Aufgaben zu entwicklen und anzupassen. Dadurch können auch Personen oder kleinere Entwicklerteams eigene Chatbots erstellen, die auf bestimmte
Anwendungsbereiche zugeschnitten sind.

Ein wichtige Anwendungsbereich ist das Sprachlernen. Chatbots können dabei als interaktive Gesprächspartner dienen und Lernenden ermöglichen, Sprachkenntnisse durch Dialoge zu üben.
Eine der bekanntesten Sprachlern-Apps ist \textbf{Duolingo}. Die Plattform nutzt unter anderem LLMs, um neue Übungen und Aufgaben zu generieren sowie personalisierte Lerninhalte für Nutzer 
bereitzustellen.\parencite{Duolingo_KI}

Das KI-Modell \textbf{Birdbrain} von Duolingo wird unter anderem zur automatischen Erstellung von Lernübungen eingesetzt.
Dabei werden LLMs durch strukturierte Prompts gesteuert, die bestimmten pädagogische und linguistische Vorgaben enthalten.
Der Prozess zur Erstellung neuer Übungen besteht aus mehreren Schritten. Zunächst werden die Inhalte einer Lektion geplant. 
Anschließend wird das KI vorbereitet, indem relevante Parameter in den Prompt intergriert werden, wie die Zielsprache, Sprachniveau, sowie das Thema der Lektion.
Die generierten Aufgabe können von den Entwicklern überprüft und bei Bedarf angepasst werden.\parencite{Duolingo_KI}

Eine neuere Funktion der Plattform ist \textbf{Duolingo Max}, ein erweitertes Lernangebot, das auf LLMs basiert. Dabei werden moderne KI-Modelle eingesetzt,
um zusätzliche interaktive Lernmöglichkeiten bereitzustellen.
Mit Duolingo Max können Nutzer ihre Sprachkenntnisse in realistischeren Kommunikationssituationen üben. Dazu gehören beispielweise simulierte Gespräche in Form von 
Rollenspielen oder Video-Anrufe mit einem virtuellen Gesprächspartner.
Die Funktion basiert auf dem Sprachmodell GPT-4, das für die Generierung von Antworten und Feedback verwendet wird.
Dadurch kann das System auf Eingaben der Lernenden flexibel reagieren und kontextabhängige Antworten erzeugen. 
Zusätzlich ermöglicht das Modell eine höhere Genauigkeit sowie schnellere Generierung von Rückmeldungen. \parencite{Duolingo_Max}

Der Einsatz von KI ermöglicht es, Lerninhalte deutlich schneller zu produzieren als mit rein manuellen Prozessen.
Da Plattformen wie Duolingo eine sehr große Anzahl an Nutzern bedienen, erleichtert diese Automatisierung die kontinuierliche Erweiterung und
Aktualisierung von Kursinhalten.

\subsection{Zielsetzung (Ronja Rüther)}
Das Ziel dieses Projekts ist die Entwicklung einer webbasierten Sprachlern-Applikation namens \enquote{Lingotrain AI}, die
die Möglichkeiten von Large Language Models nutzt, um ein individuelles Sprachtraining zu ermöglichen. Die Anwendung 
unterstützt die Zielsprachen Englisch, Französisch und Spanisch, wobei Deutsch immer als Ausgangssprache dient. 
Die Schwerpunkte der Entwicklung liegen dabei auf der dynamischen Satzgenerierung und der Evaluation der Nutzereingaben.

Ein zentraler Aspekt ist die Implementierung einer Logik, die zu einer gegebenen Vokabel dynamisch Beispielsätze generiert.
Dabei ist besonders darauf zu achten, dass die Sätze inhaltlich passend und grammatikalisch korrekt sind.
Außerdem wird ein Schwierigkeitsgrad gegeben (einfach, mittel, schwer), an den der Satz angepasst werden soll. Dieser basiert auf 
der Anzahl, wie oft der Nutzer die gegebene Vokabel bereits gelernt hat. 

Des Weiteren soll das System die Antwort des Nutzers evaluieren. Um die Lernmotivation zu steigern, ist ein zweistufiges Feedback-System
geplant. Dabei soll der Nutzer bei falschen Eingaben zunächst einen hilfreichen Hinweis erhalten, um einen zweiten Lösungsversuch zu ermöglichen.
Anschließend soll ihm detailliert Feedback gegeben werden, in dem ein Korrekturvorschlag und eine Erklärung des Fehlers enthalten ist.
Auch bei korrekten Antworten wird eine Bestätigung mit kurzem Feedback ausgegeben.

Zusätzlich zum Training mit einzelnen Sätzen ist auch ein Konversationsmodus vorgesehen. In diesem Modus soll eine vorgegebene Vokabel
aktiv in die Konversation eingebunden werden. Das Modell generiert hierfür passende Sätze, die für eine Konversation mit 
dem Nutzer geeignet sind. Auf diese Sätze muss der Nutzer reagieren, sodass ein interaktives Gespräch entsteht.
Diese Konversation findet statt, bis der Nutzer das Zielwort mehrfach erfolgreich angewendet hat oder eine maximale
Anzahl an Interaktionen erreicht ist. Abschließend analysiert das Modell die Konversation und erstellt detailliertes Feedback,
um dem Nutzer Verbesserungen sowie häufige Fehlerquellen nennen zu können.

\section{Simulationsumgebung und Trainingsdaten (Andreleene Prado)}
Die Implementierung des Systems erfolgte in der Entwicklungsumgebung Visual Studio Code unter Verwendung der Programmiersprache Python. 
Vor der Integration des Backends mit dem Frontend der Web-UI wurden die einzelnen Funktionen zunächst über das Terminal getestet, um die korrekte Verarbeitung der Eingaben und die Funktionsweise der NLP-Komponenten zu überprüfen.

Für den Satzgenerierungsmodus wurden verschiedene Textausgaben direkt im Terminal getestet. 
Dabei wurde eine Liste von Testfällen erstellt, die englische, französische und spanische Sätze umfasst. 
Zu jedem Testfall wurden sowohl der originale Referenzsatz als auch die vom Benutzer eingegebene Variante sowie die erwartete Auswertung dokumentiert. 
Diese Testfälle dienten dazu, die korrekte Verarbeitung und Bewertung der Benutzereingaben zu überprüfen.

Für den Konversationsmodus wurde eine einfache textbasierte Mini-Konversation im Terminal implementiert. 
Diese basierte auf einem festgelegten Vokabel sowie den entsprechenden englischen Übersetzungen, um den Ablauf der Interaktion und die Reaktionslogik des Systems zu testen. 
Dadurch konnte überprüft werden, ob der Chatbot auf Benutzereingaben korrekt reagiert und der Dialogfluss wie vorgesehen funktioniert.

In diesem Projekt wurden keine eigenen Trainingsdatensätze erstellt und es wurden keine Modelle von Grund auf trainiert. 
Stattdessen wurden vortrainierte NLP-Modelle verwendet, die bereits auf großen Textkorpora trainiert wurden.

Zusätzlich wurde ein Large Language Model (LLM) eingesetzt, um die Generierung von Antworten zu unterstützen. 
Dieses Modell basiert auf umfangreichen Trainingsdaten aus unterschiedlichen Textquellen und ist in der Lage, kontextabhängige sowie sprachlich kohärente Antworten zu erzeugen.

\section{Architektur (Ronja Rüther)}
In diesem Kapitel wird die Architektur der Anwendung beschrieben.
Dabei werden zunächst die grundlegende Systemarchitektur sowie die Kommunikation zwischen Frontend und Backend dargestellt. 
Anschließend erfolgt eine detailliertere Erläuterung der Implementierung des Satzmodus sowie Konversationsmodus.

\subsection{Systemarchitektur (Ronja Rüther)}
Die Architektur von \enquote{Lingotrain AI} basiert auf einem modernen \textbf{Client-Server-Modell}.
Das Frontend wurde in \textit{Vue.js} implementiert und ist für die Benutzerinteraktion sowie die Darstellung der Inhalte verantwortlich.
Aus dem Frontend werden alle Eingaben des Nutzers an das Backend weitergeleitet, welches mit \textit{FastAPI} in Python umgesetzt ist.
Das Backend übernimmt die Verarbeitung der Nutzereingaben, steuert die Anbindung an Large Language Models (LLMs) und übernimmt verschiedene Aufgaben der natürlichen Sprachverarbeitung (NLP).

Die Kommunikation zwischen dem Frontend und dem Backend erfolgt über definierte \textit{REST-API}-Endpunkte.
Die zentralen Endpunkte der Applikation sind folgende:
\begin{itemize}
    \item \texttt{/generate-sentence}: Generiert einen Beispielsatz zu einer vorgegebenen Vokabel.
    \item \texttt{/evaluate-answer}: Bewertet eine Nutzerantwort im Satzmodus auf Wortebene, d.h. die Vokabel besteht nur aus einem Wort. Es wird zudem auch Feedback und ein Korrekturhinweis geliefert.
    \item \texttt{/evaluate-sentence}: Bewertet eine Nutzerantwort im Satzmodus auf Satzebene und liefert ebenfalls Feedback.
    \item \texttt{/conversation/start}: Startet den Konversationsmodus, wobei der erste Satz für die Interaktion mit dem Nutzer generiert wird.
    \item \texttt{/conversation/next}: Führt die Konversation fort und erstellt weitere Sätze für den interaktiven Dialog.
\end{itemize}

Für die Satzgenerierung und die Bewertung werden LLMs verwendet. Dabei wird ein Prompt erstellt, der die Zielvokabel, die Zielsprache und den Schwierigkeitsgrad berücksichtigt.
Neben der LLM-basierten Bewertung kommen NLP-Tools wie \textit{SpaCy} für Tokenisierung, Lemmatisierung und POS-Tagging zum Einsatz.
Außerdem wird \textit{Sentence-BERT} zur Berechnung der semantischen Ähnlichkeit zwischen der Nutzereingabe und dem Referenzsatz genutzt.

Bevor die Eingaben an die LLMs gesendet werden, prüft das Backend die Sätze auf Plausibilität und Vollständigkeit.
Die Ergebnisse dieser Analysen werden in Echtzeit über die API an das Frontend zurückgegeben, sodass der Nutzer das Feedback direkt erhält. 
Durch diese Trennung von Frontend und Backend werden die Verantwortlichkeiten klar abgegrenzt.

\subsection{Satzmodus auf Wortebene (Ronja Rüther)}
Im Satzmodus auf Wortebene wird der Fokus darauf gelegt, dass der Nutzer einen generierten Satz zu einer Zielvokabel korrekt in die Zielsprache übersetzen kann. 
Die Implementierung erfolgt vollständig im Backend und nutzt die bereits beschriebenen NLP- und LLM-Komponenten.
Die generative KI wird daher mit den klassischen NLP-Methoden kombiniert, um eine präzise Bewertung zu gewährleisten.

\subsubsection{Implementierung der Satzgenerierung (Ronja Rüther)}
\label{implementierung_satzgenerierung}
Bei der Satzgenerierung wird nicht einfach auf eine statische Datenbank mit festen Beispielsätzen zurückgegriffen, sondern die Sätze werden zur Laufzeit zu einer gegebenen Zielvokabel über die
Funktion \texttt{generate\_sentence(...)} erzeugt. Dadurch wird der Lerninhalt variiert und der Nutzer kann immer wieder unterschiedliche Sätze zu der gleichen Vokabel lernen.

Damit die Kommunikation mit verschiedenen Sprachmodellen modellunabhängig möglich ist, wird die Bibliothek \textbf{liteLLM} als Abstraktionsschicht eingesetzt. Diese bildet die Brücke zwischen unserem 
FastAPI-Backend und dem Sprachmodell GPT-4o. 
Den größten Teil der Satzgenerierung nimmt die Promptgenerierung und damit das Design des \texttt{sentence\_prompt(...)} ein. 
Dieser Prompt steuert das Verhalten des verwendeten LLMs, sodass dem Modell strikte Regeln für verschiedene Schwierigkeitsgrade erteilt werden:
\begin{itemize}
    \item \textbf{Einfach:} Der Fokus liegt auf Hauptsätzen im Präsens ohne Adjektive.
    \item \textbf{Mittel:} Es werden Nebensätze eingeführt. Das können z.B. Kausalsätze mit \enquote{weil} sein.
    \item \textbf{Schwer:} Der Satz besteht aus komplexeren Strukturen mit variablen Zeitformen.
\end{itemize}

Ein entscheidender Aspekt des Prompt Designs ist außerdem die Verwendung von \textit{Negative Constraints}. 
Damit der generierte Satz im Backend weiterverarbeitet werden kann, wird das Modell angewiesen, keine einleitenden Sätze, Erklärungen oder Übersetzungen auszugeben.
Dadurch wird sichergestellt, dass die Antwort des Modells direkt als String-Variable verwendet werden kann, ohne dass eine aufwendige Bereinigung des Textes notwendig ist. 

Um eine hohe Variabilität der Lerninhalte sicherzustellen und zu verhindern, dass der Nutzer bei der gleichen Vokabel immer denselben Satz erhält, wurde das Modell unter Verwendung drei Parameter konfiguriert:
\begin{enumerate}
    \item \textbf{n = 5:} Es werden intern fünf verschiedene Satzvarianten generiert. Aus dieser Auswahl wird dann mittels einer Zufallsfunktion ein Satz extrahiert und an das Frontend übermittelt.
    \item \textbf{temperature = 0.9:} Durch diesen Wert wird die Kreativität des Modells gesteigert. Ein höherer Wert sorgt dafür, dass die Wortfolgen weniger vorhersehbar gewählt werden.
    \item \textbf{top\_p = 0.9:} Das sogenannte \textit{Nucleus Sampling} ist dafür zuständig, dass die grammatikalische Stabilität trotz der hohen Kreativität des Modells erhalten bleibt. Dafür wird die Wortauswahl auf die wahrscheinlichsten 90\% der Wortkandidaten beschränkt und dadurch verhindert, dass das Modell zu abwegige Begriffe verwendet.
\end{enumerate}

Durch die Kombination dieser Parameter wird ein guter Mittelweg für die didaktische Korrektheit und die Abwechslung in den Sätzen erzielt.

\subsubsection{Hybride Evaluation und Feedback-Logik (Ronja Rüther)}
Die größte Herausforderung des Satzmodus stellt die Bewertung der Nutzereingaben dar. Da die Nutzer die Zielwörter oft konjugieren oder leicht vom Original abweichen, würde ein einfacher String-Vergleich zu einer hohen Fehlerrate führen.
Um die Genauigkeit der Korrektur zu erhöhen und sich nicht nur auf das LLM zu verlassen, aber auch um Kosten zu sparen und schnellere Antworten liefern zu können, wurde die Bewertung in zwei Stufen aufgeteilt.
Bei dieser hybriden Evaluation gibt es zunächst eine regelbasierte Vorprüfung und anschließend eine semantische Analyse mittels LLM.

\textbf{Regelbasierte Vorprüfung mittels SpaCy:} \\
Zunächst wird die Antwort des Nutzers lokal im Backend analysiert, bevor ein Aufruf des LLMs erfolgt. Dies hat den Zweck, Kosten zu sparen, da jeder LLM-Aufruf Kosten verursacht.
Bei der Vorprüfung im Backend wird die NLP-Bibliothek \textit{SpaCy} verwendet.

Den ersten Schritt bei dieser regelbasierten Vorprüfung bildet die \textbf{Lemmatisierung}. Die Funktion \texttt{contains\_target\_word(...)} reduziert dabei die Zielvokabel 
und den eingegebenen Satz des Nutzers auf ihre Grundformen (Lemmata). Dadurch wird sichergestellt, dass beispielsweise die Eingabe \enquote{went} als korrekte Verwendung 
der Vokabel \enquote{go} erkannt wird.

Damit auch Mehrwort-Vokabeln, wie z.B. \enquote{ice cream}, erkannt und identifiziert werden können, prüft ein Algorithmus die Wortgruppen innerhalb eines Satzes.
Dadurch wird verhindert, dass korrekte Antworten aufgrund von Leerzeichen in der Eingabe fälschlicherweise als falsch bewertet werden.

Da ein eingegebener Satz auch mit einem Satzzeichen enden soll, wird vorab mithilfe der Funktion \texttt{check\_sentence\_end(...)} geprüft, ob das letzte Zeichen im Satz ein Punkt, Ausrufezeichen oder Fragezeichen enthält. Nachfolgende Leerzeichen werden dabei durch Verwendung der \texttt{.strip()}-Funktion ignoriert.

Zuletzt wird durch das \textit{POS-Tagging} (Part-of-Speech) geprüft, ob die Eingabe des Nutzers ein Verb enthält und somit einen vollständigen Satz darstellt. 
Dazu wurde die Funktion \texttt{is\_sentence\_plausible(...)} implementiert, die linguistische Merkmale von \textit{SpaCy} nutzt, um die Verben und Hilfsverben zu identifizieren. 
Dabei wurde ein zweistufiger Check implementiert, der verhindert, dass bloße Wortlisten oder unvollständige Sätze an das LLM weitergeleitet werden:
\begin{enumerate}
    \item \textbf{POS-Tagging und Morphologie:} Das System prüft jedes Token auf sein POS-Tag (\texttt{VERB}) oder (\texttt{AUX}). Zusätzlich werden morphologische Merkmale wie Zeitform (\textit{Tense}) oder Personalform (\textit{Person}) abgefragt. Dies ist für die Sprachen Französisch und Spanisch besonders wichtig, um komplex konjugierte Verben nicht zu übersehen.
    \item \textbf{Dependenz-Analyse:} Es wird zusätzlich geprüft, ob ein Wort als Wurzel (\texttt{dep\_ == \enquote{ROOT}}), also zentrales Element des Satzes, gilt, von dem alle anderen Wörter abhängen. Dadurch können auch Sätze korrekt erkannt werden, bei denen die automatische Wortart-Erkennung unsicher ist.
\end{enumerate} 
\bigskip
\textbf{Semantische Analyse und Feedback:}  \\
Sind die regelbasierten Vorprüfungen ohne Fehler durchlaufen, wird das LLM zur finalen semantischen Prüfung verwendet. 
Das Modell kann dabei durch sein Sprachverständnis beurteilen, ob die Eingabe des Nutzers die Bedeutung des Referenzsatzes korrekt wiedergibt.
Im \texttt{evaluation\_prompt(...)} wird das Modell dabei angewiesen, die Bedeutung der Nutzerantwort mit dem Referenzsatz zu vergleichen. 

Auf Basis der Bewertung soll das Modell auch Feedback für den Nutzer generieren. Um die Frustration bei kleineren Fehlern zu minimieren, wird ein zweistufiger Ansatz verfolgt.
Wenn das Modell ein fehlerhaftes Satzzeichen oder eine minimale Abweichung erkennt, vergibt das Modell ein Rating von \texttt{almost\_correct}. Der Nutzer erhält in diesem Fall einen Tipp (Hint),
ohne dass sofort die vollständige Lösung eingeblendet wird. Auch bei ganz falschen Antworten (\texttt{rating = \enquote{wrong}}) wird so vorgegangen.
Erst im zweiten Versuch wird eine korrekte Antwort sowie eine technische Begründung des Fehlers geliefert.

Um die Weiterverarbeitung im Backend sicherzustellen, wird das Modell angewiesen, die Antwort als JSON-Objekt zurückzugeben.
Dieses Objekt enthält dann die Felder für das \texttt{rating}, einen Korrekturvorschlag (\texttt{corrected\_sentence}), einen Hinweis (\texttt{hint}) sowie ein
kurzes Feedback (\texttt{short\_feedback}) und die technische Begründung (\texttt{comment}). Für die Speicherung des Ergebnisses in der Datenbank wird außerdem ein 
Wahrheitswert erwartet (\texttt{semantically\_correct}). Durch diese Strukturierung kann die logische Bewertung von der textuellen Rückmeldung an den Nutzer klar getrennt werden.

\subsection{Satzmodus auf Satzebene (Andreleene Prado)}
Da die Vokabelliste nicht nur einzelne Wörter, sondern auch vollständige Sätze enthält, müssen diese ebenfalls bei der Evaluation berücksichtigt werden. 
Im Frontend werden vollständige Sätze jedoch nicht an das Backend zur Satzgenerierung gesendet, sondern direkt ausgegeben. 
Der Grund dafür ist, dass der Satz bereits vollständig vorliegt und eine erneute Generierung durch ein Large Language Model (LLM) keinen zusätzlichen Nutzen bringen würde. 
Dies würde lediglich zu einer unnötigen Nutzung von Rechenressourcen führen.

Für die Bewertung von Sätzen wird ein eigener REST-API-Endpunkt verwendet, der sich von dem Endpunkt für einzelne Wörter unterscheidet. 
Die Auswertung erfolgt in der Funktion \texttt{evaluate\_sentence\_similarity}. 
Als Eingabeparameter erhält diese Funktion den vom Benutzer eingegebenen Satz sowie den Referenzsatz, der als korrekte Übersetzung in der Datenbank gespeichert ist. 
Die Parameter \texttt{target\_word} und \texttt{target\_language} sind für den Satzmodus nicht erforderlich. 
Sie sind jedoch weiterhin Bestandteil des \texttt{EvalRequest}-Modells, da dieses ursprünglich für die Evaluation einzelner Wörter entwickelt wurde.

Zunächst werden sowohl der eingegebene Satz als auch der Referenzsatz normalisiert. 
Dazu wird die Hilfsfunktion \texttt{normalize()} verwendet. 
In diesem Schritt wird der Text in Kleinbuchstaben umgewandelt, führende und nachfolgende Leerzeichen werden entfernt und Satzzeichen werden gelöscht. 
Diese Normalisierung erleichtert den Vergleich der beiden Sätze, da Unterschiede in Groß- und Kleinschreibung oder Interpunktion keinen Einfluss auf die Bewertung haben.

Nach der Normalisierung wird zunächst geprüft, ob der eingegebene Satz exakt mit dem Referenzsatz übereinstimmt. 
Ist dies der Fall, wird die Antwort als korrekt bewertet. 
Diese Vorgehensweise setzt voraus, dass die in der Datenbank gespeicherten Sätze inhaltlich sowie grammatikalisch korrekt sind. 
Wenn der Benutzer den Satz exakt reproduziert, wird dieser als korrekt erkannt und entsprechend als gelernt markiert.

Falls keine exakte Übereinstimmung vorliegt, wird die semantische Ähnlichkeit zwischen beiden Sätzen berechnet. 
Ziel ist es zu überprüfen, ob der vom Benutzer eingegebene Satz denselben Sinn wie der Referenzsatz ausdrückt, auch wenn die Formulierung leicht abweicht.

Für diese Berechnung wird ein Sentence-BERT-Modell (SBERT) verwendet. 
SBERT basiert auf der BERT-Architektur und ist speziell dafür optimiert, semantische Ähnlichkeiten zwischen Sätzen zu bestimmen. 
Dabei werden beide Sätze zunächst in sogenannte Embeddings umgewandelt, also in numerische Vektorrepräsentationen im semantischen Vektorraum. 
Anschließend wird die Cosinus-Ähnlichkeit zwischen diesen Vektoren berechnet.

Der resultierende Ähnlichkeitswert liegt zwischen 0 und 1, wobei höhere Werte eine stärkere semantische Übereinstimmung anzeigen. 
Abhängig vom berechneten Wert wird die Benutzereingabe in verschiedene Kategorien eingeteilt:

\begin{itemize}
\item \textbf{Ähnlichkeit $\geq 0.85$:} Der Satz ist sinngleich mit dem Referenzsatz und wird als nahezu korrekt bewertet.
\item \textbf{Ähnlichkeit $\geq 0.65$:} Der Satz ist teilweise korrekt, enthält jedoch kleinere Abweichungen.
\item \textbf{Ähnlichkeit $< 0.65$:} Der Satz wird als falsch bewertet.
\end{itemize}

Zusätzlich wird dem Benutzer Feedback gegeben, das den Ähnlichkeitswert sowie einen Verbesserungsvorschlag enthält. 
Als Vorschlag wird dabei der korrekte Referenzsatz aus der Datenbank angezeigt, sodass der Benutzer seine Eingabe direkt mit der korrekten Formulierung vergleichen kann.

\subsection{Konversationsmodus (Andreleene Prado)}
Im Konversationsmodus werden alle Wörter eines Lernsets berücksichtigt. 
Zu Beginn einer neuen Konversation wird im Frontend zufällig eine Vokabel aus der ausgewählten Lernsetliste ausgewählt. 
Diese wird anschließend zusammen mit der Zielsprache und der Übersetzung über einen REST-API-Endpunkt an das Backend gesendet.

Die Konversation wird durch die Funktion \texttt{start\_conversation()} gestartet. 
Die wichtigsten Parameter sind das Zielwort (\texttt{target\_word}), die Zielsprache (\texttt{target\_language}) sowie die Übersetzung (\texttt{translation}). 
Diese Informationen werden verwendet, um einen geeigneten Prompt für das Large Language Model (LLM) zu erstellen. 
Die Übersetzung des Wortes ist dabei wichtig, damit das Modell den semantischen Kontext des Wortes korrekt interpretieren kann.

Zusätzlich enthält der Prompt bestimmte Regeln, die sich an der Wortart des Fokuswortes orientieren. 
Dadurch wird sichergestellt, dass der generierte Dialog den Lernenden dazu anregt, das Zielwort aktiv zu verwenden.

\textbf{Wortartabhängige Regeln:}
\begin{itemize}
    \item \textbf{Verb:} Der Assistent stellt Fragen zu Handlungen oder Aktivitäten und fragt nach persönlichen Erfahrungen oder Gewohnheiten, z.\,B. „Wann machst du das?“ oder „Warum machst du das?“
    \item \textbf{Nomen:} Der Assistent stellt Fragen, bei denen der Lernende das Nomen natürlich als Objekt verwenden kann, z.\,B. durch Fragen nach Besitz, Meinungen oder Erfahrungen.
    \item \textbf{Adjektiv:} Der Assistent stellt Fragen zu Meinungen oder Beschreibungen, z.\,B. „Findest du das auch so?“ oder „Wann ist etwas so?“
\end{itemize}

Das LLM generiert anschließend die erste Frage der Konversation, welche an das Frontend zurückgegeben und dort im Chat angezeigt wird.

Wenn der Benutzer antwortet, wird die Funktion \texttt{next\_turn()} aufgerufen. 
Diese erhält den aktuellen Konversationszustand (\texttt{state}), die Benutzereingabe (\texttt{user\_input}) sowie die Zielsprache als Parameter.

Zunächst wird die Benutzereingabe mithilfe eines spaCy-Sprachmodells analysiert. 
Dabei wird überprüft, ob die Eingabe einen plausiblen vollständigen Satz enthält. 
Falls kein Verb vorhanden ist oder die Eingabe nicht als vollständiger Satz interpretiert werden kann, wird der Benutzer aufgefordert, einen vollständigen Satz zu formulieren.

Anschließend wird geprüft, ob die Antwort inhaltlich zur letzten Frage des Assistenten passt. 
Dazu wird die letzte Assistentenfrage aus dem Chatverlauf extrahiert und zusammen mit der Benutzereingabe an das LLM übergeben. 
Das Modell bewertet anhand eines speziellen Prompts, ob die Antwort relevant ist. 
Diese Prüfung verhindert, dass der Benutzer lediglich einen beliebigen Satz mit dem Zielwort schreibt, ohne auf die gestellte Frage einzugehen.

Wenn beide Prüfungen erfolgreich sind, wird die Benutzereingabe akzeptiert und im Konversationszustand gespeichert. 
Anschließend wird analysiert, wie häufig das Zielwort im bisherigen Gespräch verwendet wurde. 
Hierfür wird der Chatverlauf mit einem spaCy-Modell verarbeitet und mithilfe der Funktion \texttt{contains\_target\_word()} geprüft, ob das Zielwort in den Benutzernachrichten vorkommt.

Die Anzahl der Wortverwendungen (\texttt{usage\_count}) sowie die Anzahl der bisherigen Gesprächsrunden (\texttt{turn\_count}) werden im Konversationszustand gespeichert und kontinuierlich aktualisiert.

Die Konversation endet unter zwei möglichen Bedingungen:
\begin{itemize}
    \item Wenn das Zielwort mindestens zweimal vom Benutzer verwendet wurde und der Benutzer mindestens drei Antworten im Dialog gegeben hat. 
    In diesem Fall wird davon ausgegangen, dass das Wort ausreichend geübt wurde.
    \item Wenn der Benutzer zehn Gesprächsrunden erreicht hat, ohne die erste Bedingung zu erfüllen. 
    In diesem Fall wird die Konversation beendet, um zu vermeiden, dass der Dialog unnötig lange fortgesetzt wird.
\end{itemize}

Nach Beendigung der Konversation wird der gesamte Chatverlauf vom LLM bewertet. 
Hierzu wird die Funktion \texttt{evaluate\_conversation()} verwendet, die den Verlauf an das LLM übergibt und ein strukturiertes Feedback generieren lässt. 
Dieses Feedback enthält eine Bewertung der Konversation, einen kurzen Kommentar zur Sprachverwendung sowie gegebenenfalls Hinweise zur Verbesserung.

Um die Kosten der LLM-Anfragen zu reduzieren und gleichzeitig den relevanten Kontext zu erhalten, wird der Chatverlauf vor jeder Anfrage gekürzt. 
Dabei bleiben die Systemnachricht sowie maximal fünf der letzten Nachrichten im Verlauf erhalten. 
Diese Begrenzung stellt sicher, dass das Modell ausreichend Kontext erhält, ohne unnötig viele Tokens zu verarbeiten.

Nach Abschluss einer Konversation wird im Frontend automatisch eine neue Konversation mit einer anderen zufällig ausgewählten Vokabel gestartet. 
Die zuvor verwendete Vokabel wird aus der Liste entfernt, sodass sie nicht erneut in derselben Lernsession verwendet wird.

\section{Evaluation der Ergebnisse}

\subsection{Evaluation des Satzmodus auf Wortebene (Ronja Rüther)}
Zur Evaluation des Satzmodus auf Wortebene wurden die Satzgenerierung und die Bewertung der Nutzereingaben analysiert.
Da die Satzgenerierung ein generatives Verfahren ist, existiert für einen generierten deutschen Satz keine eindeutig korrekte Übersetzung.
Der Nutzer kann daher für den gleichen Satz verschiedene Übersetzungen eingeben, die als semantisch korrekt anerkannt werden. Aus diesem Grund 
wurde die Evaluation der Ergebnisse in die Analyse der generierten Sätze sowie die Evaluation der Bewertung aufgeteilt.

\subsubsection{Evaluation der Satzgenerierung (Ronja Rüther)}
Zur Evaluation der Satzgenerierung wurde ein Testskript in der Datei \textit{test\_llm.py} implementiert, das die Funktion \texttt{generate\_sentence(...)}
mit verschiedenen Zielvokabeln, Zielsprachen und Schwierigkeitsgraden aufruft. 
Das Ziel dieser Tests war es, zu überprüfen, ob sinnvolle und passende Sätze vom Modell erzeugt werden.
Dabei bestand ein besonderer Fokus auch darin, zu überprüfen, ob der Schwierigkeitsgrad des Satzes korrekt beachtet wurde und keine zu komplexen Sätze erzeugt wurden.

Da es sich bei der Satzgenerierung, wie bereits erwähnt, um ein generatives Verfahren handelt, existiert für eine gegebene Zielvokabel keine eindeutig korrekte Lösung.
Mehrere unterschiedliche Sätze können semantisch korrekt sein und die Zielvokabel in einem passenden Kontext verwenden.
Daher wurde die Qualität der generierten Sätze anhand mehrerer Kriterien beurteilt.

Zunächst wurde bei der Bewertung überprüft, ob die Zielvokabel im generierten Satz vorhanden und korrekt verwendet wird.
Zusätzlich wurde analysiert, ob die Sätze grammatikalisch plausibel sind und die Zielvokabel im korrekten Kontext verwendet wird.
Ein weiterer wichtiger Aspekt war die Anpassung der Satzstruktur an den gewählten Schwierigkeitsgrad.

Für die Tests wurden mehrere Beispielvokabeln aus unterschiedlichen Sprachen verwendet.
Dabei wurde für jede Vokabel jeweils ein Satz für alle möglichen Schwierigkeitsgrade \enquote{einfach}, \enquote{mittel} und \enquote{schwer} generiert.
Die Tabelle \ref{tabelle_satzgenerierung} zeigt eine Auswahl der generierten Beispielsätze.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{|l|l|l|X|X|X|}
\hline
\textbf{Vokabel} & \textbf{Übers.} & \textbf{Zielsprache} & \textbf{Einfach} & \textbf{Mittel} & \textbf{Schwer} \\
\hline
Birne & pear & Englisch & Ich esse eine Birne. & Ich esse gerne Birnen, weil sie süß und saftig sind. & Nachdem ich die Birne gegessen hatte, war ich nicht mehr hungrig. \\
\hline
Flugzeug & avion & Französisch & Das Flugzeug fliegt am Himmel. & Das Flugzeug startet, aber es regnet draußen. & Obwohl das Flugzeug spät abflog, landete es pünktlich am Zielort. \\
\hline
laufen & correr & Spanisch & Ich laufe jeden Morgen im Park. & Ich laufe schnell, weil ich den Bus erwischen will. & Nachdem die Sonne untergegangen ist, laufen wir schnell den Hügel hinunter. \\
\hline
\end{tabularx}
\caption{Beispiel generierter Sätze für verschiedene Schwierigkeitsgrade}
\label{tabelle_satzgenerierung}
\end{table}

Die Ergebnisse zeigen, dass bei einfachen Schwierigkeitsgraden überwiegend kurze Hauptsätze generiert werden.
Beim Schwierigkeitsgrad \enquote{mittel} entstehen hingegen Sätze mit einfachen Nebensätzen und zusätzlichem Adjektiv.
Ist der Schwierigkeitsgrad auf \enquote{schwer} gesetzt, so werden auch komplexere Satzstrukturen mit mehreren Satzteilen verwendet.
Außerdem wird die Zeitform variiert. Die Sätze sind auch nicht zu schwer, sondern für einen Lernenden mit fortgeschrittener Erfahrung lösbar.

Die Funktion wurde zudem auch mehrfach mit der gleichen Vokabel aufgerufen, um die Variation der Sätze zu überprüfen.
Dabei zeigte sich, dass das Sprachmodell in den meisten Fällen verschiedene Formulierungen generiert und dadurch verhindert, dass bei 
Wiederholungen dem Nutzer nicht immer die gleichen Sätze angezeigt werden.

Insgesamt zeigt die Evaluation, dass das Modell grammatikalisch plausible Sätze generiert und die Komplexität der Satzstruktur erfolgreich an den gewählten
Schwierigkeitsgrad angepasst wird. Der Satz wird auch im korrekten Kontext für die Zielvokabel verwendet, sodass beim Wort \enquote{Birne} aufgrund der angegebenen 
Übersetzung (in der Tabelle mit Übers. abgekürzt) \enquote{pear} ein Satz in Bezug auf das Obst und nicht die Lichtquelle generiert wird.

Da die Tests nur eine begrenzte Anzahl von Vokabeln und Beispielsätzen enthalten, kann keine statistisch signifikante Aussage über die allgemeine 
Qualität des Modells getroffen werden. Bei weiteren durchgeführten Tests über das Chatbot-UI wurden jedoch auch akzeptable Sätze generiert.

\subsubsection{Evaluation der Bewertung (Ronja Rüther)}

\section{Kritische Reflexion und Fazit}
\subsection{Aufgetretene Probleme}
\subsubsection{Zu komplexe Satzstrukturen bei hohem Schwierigkeitsgrad (Ronja Rüther)}
Eine Herausforderung bei der Satzgenerierung bestand in der Anpassung der Sätze an den Schwierigkeitsgrad. 
Besonders beim Schwierigkeitsgrad \enquote{schwer} erzeugte das Sprachmodell teilweise sehr lange und stark verschachtelte Sätze.
Diese enthielten häufig mehrere Nebensätze und verfolgten einen sehr poetischen Stil mit schweren zusätzlichen Wörtern, wodurch die Übungen für den Nutzer unnötig schwierig wurden.

Um dieses Problem zu beheben, wurde das Prompt-Design mehrfach überarbeitet und dem Modell präzisere Anweisungen gegeben, um die Satzlänge zu begrenzen und die Struktur der Sätze zu begrenzen.
Zusätzlich wurde der Fokus auch mehr auf die klare und verständliche Generierung des Satzes gelegt.

Durch diese Anpassung konnte erreicht werden, dass im Schwierigkeitsgrad \enquote{schwer} die Sätze anspruchsvoller werden, aber 
dennoch verständlich und für einen Lernenden lösbar bleiben.

\subsubsection{Problem bei der Erkennung konjugierter Wortformen (Ronja Rüther)}
Bei der Evaluation der Nutzereingaben trat ein weiteres Problem bei der Erkennung bestimmter Wortformen auf.
Besonders im Spanischen wurden korrekt verwendete Wörter teilweise als falsch bewertet. Dazu zählte beispielsweise das 
Wort \enquote{como}, welches eine konjugierte Form des Verbs \enquote{comer} darstellt.

Zu Beginn wurde bei der Überprüfung, ob die Zielvokabel im Satz vorkommt, hauptsächlich ein direkter Vergleich der eingegebenen Wörter mit der Zielvokabel durchgeführt.
Dadurch wurden konjugierte Formen nicht zuverlässig erkannt.

Zur Lösung dieses Problems wurde die Evaluation um eine weitere Analyse ergänzt, die \textit{SpaCy} nutzt, um Lemmatisierung durchzuführen und das Wort auf die Grundform zu reduzieren.
Wie bereits im Anschnitt \ref{implementierung_satzgenerierung} beschrieben, wird dabei nun auch insbesondere das Wurzelelement des Satzes (\texttt{ROOT}) berücksichtigt.
Dadurch lassen sich auch in der spanischen Sprache die konjugierten Formen zuverlässiger erkennen und bewerten.

\subsubsection{Fehlerhafte Erkennung von Satzzeichen (Ronja Rüther)}
Ein weiteres Problem trat bei der Bewertung der Satzzeichen auf. Zu Beginn bewertete das Sprachmodell Sätze häufig als fehlerhaft (\texttt{rating = \enquote{almost\_correct}}), 
da das Satzzeichen fehlen würde, obwohl die Nutzereingabe mit dem korrekten Satzzeichen endete.

Die Ursache bestand darin, dass die Bewertung der Satzzeichen vollständig durch das Sprachmodell erfolgte. In einigen Fällen hat dabei das Modell das vorhandene Satzzeichen allerdings ignoriert.

Um dieses Problem zu lösen, wurde eine zusätzliche regelbasierte Vorprüfung im Backend implementiert.
Die Funktion \texttt{check\_sentence\_end(...)} prüft vor dem LLM-Aufruf, ob der Satz mit einem gültigen Satzzeichen endet.
Ist das der Fall, prüft das LLM anschließend nur noch, ob das verwendete Satzzeichen im Kontext des Satzes passend ist, aber nicht mehr, ob überhaupt ein Satzzeichen vorhanden ist. 

\subsubsection{Problem bei der Behandlung von Sätzen im Satzmodus (Andreleene Prado)}
Im Satzmodus werden normalerweise zufällige Vokabeln aus dem Lernset ausgewählt und an das LLM gesendet, damit daraus ein Satz generiert wird, den der Benutzer anschließend übersetzen soll. 
Dabei trat jedoch ein besonderes Problem bei Vokabeln auf, die bereits vollständige Sätze enthalten.

Da diese Sätze bereits vollständig formuliert sind, war zunächst vorgesehen, sie bei der Generierung zu überspringen. 
Dieser Ansatz führte jedoch zu einem unerwarteten Problem im Lernablauf. 
Wenn Sätze übersprungen wurden, blieben sie weiterhin als offene Einträge in der Datenbank bestehen. 
Dadurch konnte eine Lernrunde nicht korrekt abgeschlossen werden, da immer noch unbearbeitete Sätze vorhanden waren, die dem Benutzer jedoch nie angezeigt wurden.

Aus diesem Grund wurde die Implementierung angepasst. 
Anstatt die Sätze zu überspringen, werden diese nun direkt im Frontend angezeigt und der Benutzer wird aufgefordert, die entsprechende Übersetzung einzugeben. 
Die Bewertung erfolgt anschließend im Backend durch einen Vergleich mit dem in der Datenbank gespeicherten Referenzsatz.

Eine weitere Herausforderung bestand darin, dass Benutzer einen Satz häufig in einer leicht abweichenden Formulierung eingeben, obwohl die Bedeutung korrekt ist. 
Das Ziel des Chatbots ist jedoch nicht, eine exakt identische Formulierung zu erzwingen, sondern zu überprüfen, ob der Benutzer den Inhalt des Satzes korrekt verstanden hat.

Aus diesem Grund wurde zusätzlich eine semantische Ähnlichkeitsbewertung implementiert. 
Hierbei wird mithilfe eines Sentence-BERT-Modells (SBERT) die semantische Ähnlichkeit zwischen dem eingegebenen Satz und dem Referenzsatz berechnet (siehe Abschnitt 3.3). 
Dadurch können auch sinngleiche, aber unterschiedlich formulierte Antworten als teilweise oder nahezu korrekt erkannt werden. 
Dies ermöglicht ein realistischeres Feedback für den Benutzer und unterstützt den Lernprozess.

Eine weitere Herausforderung bestand in der Festlegung geeigneter Grenzwerte für die Bewertung der Ähnlichkeit. 
Es musste definiert werden, ab welchem Ähnlichkeitswert zwei Sätze als sinngleich gelten, wann sie nur teilweise korrekt sind und wann eine Antwort als falsch bewertet wird. 
Diese Schwellenwerte wurden im Rahmen mehrerer Tests empirisch bestimmt, um ein ausgewogenes Verhältnis zwischen Genauigkeit und Flexibilität zu erreichen.

\subsubsection{Problem bei der Generierung der Dialoge (Andreleene Prado)}
Im Konversationsmodus wird das zu übende Wort zusammen mit der entsprechenden Übersetzung an das LLM übergeben. 
Zu Beginn des Projekts war der Prompt relativ einfach formuliert. 
Das Modell wurde lediglich angewiesen, eine Frage zu generieren, die den Benutzer dazu auffordert, einen Satz mit dem vorgegebenen Wort zu bilden.

Während der Testphase zeigte sich jedoch, dass die generierten Fragen häufig schwer mit dem Zielwort zu beantworten waren. 
In einigen Fällen passte die Frage nicht zum Kontext oder zur Wortart des Fokuswortes, sodass der Benutzer das Wort nur unnatürlich oder gar nicht in seine Antwort integrieren konnte. 
Beispielsweise konnte eine sehr allgemeine Frage entstehen, bei der die Verwendung des Zielwortes grammatikalisch oder semantisch unpassend war.

Um dieses Problem zu lösen, wurde der Prompt schrittweise angepasst und erweitert. 
Dabei wurden zusätzliche Regeln eingeführt, die sich an der Wortart des Fokuswortes orientieren. 
Das Modell erhält nun explizite Anweisungen (siehe Abschnitt 3.4.), wie Fragen formuliert werden sollen, damit der Lernende das Zielwort möglichst natürlich in seiner Antwort verwenden kann.

Diese Anpassung des Prompt Designs führte zu deutlich konsistenteren und lernorientierteren Dialogen. 
Die generierten Fragen waren besser auf das jeweilige Wort abgestimmt und erleichterten es dem Benutzer, vollständige und sinnvolle Sätze mit dem Zielwort zu formulieren.

Das Beispiel zeigt, dass eine präzise Gestaltung von Prompts eine wichtige Rolle beim Einsatz von Large Language Models spielt.

\subsubsection{Festlegung der Abbruchbedingungen im Konversationsmodus (Andreleene Prado)}
Eine weitere Herausforderung im Konversationsmodus bestand in der Festlegung geeigneter Abbruchbedingungen für die Konversation. 
Zu Beginn der Implementierung war lediglich eine einzige Bedingung vorgesehen.
Die Konversation sollte beendet werden, sobald das Zielwort mindestens dreimal vom Benutzer verwendet wurde und der Benutzer dem Chatbot mindestens dreimal geantwortet hatte.

Während der Testphase zeigte sich jedoch, dass diese Bedingung für Lernende teilweise schwer zu erfüllen ist. 
Insbesondere bei schwierigeren oder weniger geläufigen Wörtern fiel es einigen Nutzern schwer, das Zielwort mehrfach in passenden Sätzen zu verwenden. 
In solchen Fällen konnte die Konversation sehr lange andauern oder theoretisch sogar unbegrenzt fortgesetzt werden, wenn der Benutzer das Wort nicht ausreichend oft einsetzte.

Eine solche Situation kann für Lernende frustrierend oder demotivierend wirken, da der Dialog nicht abgeschlossen wird und kein abschließendes Feedback erfolgt. 
Aus diesem Grund wurde eine zusätzliche Abbruchbedingung eingeführt.

Neben der ursprünglichen Bedingung wird die Konversation nun auch beendet, wenn der Benutzer insgesamt zehn Antworten gegeben hat. 
In diesem Fall wird die Unterhaltung unabhängig von der Häufigkeit der Wortverwendung beendet und ein Feedback zur Konversation generiert.

Durch diese zusätzliche Begrenzung wird sichergestellt, dass jede Konversation innerhalb einer überschaubaren Anzahl von Interaktionen abgeschlossen wird. 
Dies verbessert die Benutzererfahrung und verhindert, dass Lernende in einer zu langen oder stagnierenden Konversation feststecken.

\subsection{Vergleich zum Stand der Technik}

\subsection{Fazit und Ausblick}